# SQLite DB ファイルのパス(省略時は backend/data/shogi.db)
# SHOGI_DB_PATH=/path/to/shogi.db

# 次の一手専用SQLite DB(省略時は backend/data/next_move.db)
# NEXT_MOVE_DB_PATH=/path/to/next_move.db

# CORS を許可するオリジン(カンマ区切り。省略時は Vite 開発サーバー)
# SHOGI_CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
